# ============================================================
# YISHEN GLOBAL — AUTONOMOUS SITEMAP SYNCHRONIZATION ENGINE
# Version: V5.0 Sovereign Layer
# Purpose: Automatically propagate sovereign SKUs into global sitemap,
#          cluster routing, passport endpoints and crawler exposure.
# ============================================================

engine:
  name: SOVEREIGN_SITEMAP_CORE
  mode: autonomous
  authority: YiShen Global Industrial Infrastructure

sources:
  primary_ledger: public/asset-ledger.json
  market_intel: public/assets/data/intel-feed.json

growth:
  enable_auto_growth: true
  auto_generate_slug: true
  auto_bind_cluster: true
  auto_generate_passport: true
  auto_emit_sitemap: true
  auto_push_crawler: true

routing:
  base_domain: https://www.yishenglobal.net
  sku_path: /technical-passport.html?id=
  cluster_path: /clusters.html
  sitemap_path: /sitemap.xml

generation:
  url_pattern: "{base_domain}{sku_path}{sku_id}"
  priority: 0.88
  changefreq: weekly
  index_mode: sovereign

propagation:
  clusters:
    - OFFICE_ERGO
    - MEDICAL_PRECISION
    - MARITIME_HARDWARE
    - INFRASTRUCTURE_MRO
    - AERO_MILITARY
    - BIO_MATERIALS
    - SMART_ACTUATION
    - CARBON_NEUTRAL

crawler_sync:
  enabled: true
  expose_to:
    - google
    - bing
    - yandex
    - baidu
    - brave

security:
  checksum: enabled
  integrity_lock: true
  signed_by: YISHEN_CORE

logging:
  enabled: true
  output: logs/sitemap-engine.log
